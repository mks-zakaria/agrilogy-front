// Registers @testing-library/jest-dom matcher types (toBeInTheDocument, etc.)
// globally for all test files, matching the runtime import in
// jest.setup.after.js.
import '@testing-library/jest-dom';
